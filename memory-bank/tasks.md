# Tasks

## TODO
- Этап 1: собрать экран поиска
- Этап 1: собрать экран всех встреч
- Этап 2: собрать экран интеграций
- Этап 3: собрать экран настроек
- Зафиксировать и не ломать неизменный каркас (включая боковое меню)

## IN-PROGRESS
- Нет активных задач

## DONE
- 2026-02-19: стартовый экран приложения переключен с `Поиск` на `Встречи` (инициализация `screen` в `App` теперь `home`); `npm run build` успешна
- 2026-02-19: реализована фильтрация по `Дата` в popover `Фильтры` по Figma-состояниям (`default`, `single`, `range`): добавлен календарный подпоповер с выбором месяца, выбором одной даты/диапазона, кнопкой `Выбрать` и подписью выбранного значения; фильтрация подключена в `App`/`utils/filters`, обновлены счетчик активных фильтров и `Очистить фильтры`; `npm run build` успешна
- 2026-02-18: создан базовый `memory-bank` (`project.md`, `product.md`, `decisions.md`, `tasks.md`)
- 2026-02-18: `memory-bank` заполнен по продуктовому контексту `mymeet.ai` от дизайнера
- 2026-02-18: навигация переведена на единый реестр экранов без ломки текущих `search/home/filters`
- 2026-02-18: добавлены базовые экраны-заглушки `integrations` и `settings` для безопасного масштабирования
- 2026-02-18: экран `Встречи` обновлен под Figma (`27141:2899`) с табами и расширенными вариантами строк
- 2026-02-19: на экране `Встречи` добавлен контрол `Фильтры` справа в строке табов (по паттерну экрана `Поиск`)
- 2026-02-19: реализован единый выпадающий popover `Фильтры` по Figma (`27161:8402`) для экранов `Поиск` и `Встречи`; отдельный экран `filters` выведен из потока навигации
- 2026-02-19: обновлены mock-данные встреч (10 шт.) с разнообразными источниками, названиями, датами, временем и длительностью для реалистичной проверки фильтров на экранах `Поиск` и `Встречи`
- 2026-02-19: в правой колонке списка встреч отображение переведено со статуса обработки на источник встречи (иконка + название источника) по всем mock-встречам
- 2026-02-19: для встречи `Разбор UX-замечаний по фильтрам` включено состояние превью `processing` по Figma-узлу `27205:7313` (темная маска, левая светлая плашка, бейдж `55%`)
- 2026-02-19: реализована механика фильтрации по `Источник` (multi-select) для `Поиск` и `Встречи`: активный чекбокс (синий + галочка), синяя точка у `Источник`, бейдж `1` у кнопки `Фильтры`, рабочий `Очистить фильтры`
- 2026-02-19: реализована механика фильтрации по `Авторы` по образу `Источник` (тот же popover, размеры, отступы, чекбоксы и hover-поведение); бейдж у `Фильтры` теперь считает число активных типов фильтрации (например, `Источник` + `Авторы` = `2`)
- 2026-02-19: одинаковые фото-аватары участников заменены на закрепленные цветовые аватары (детерминированно по участнику) в списке встреч и в подпоповере фильтра `Авторы`; фильтрация по авторам учитывает `participant` и `authors`

- 2026-02-19: подготовлена публикация на GitHub Pages: добавлены `.gitignore`, `base` в `vite.config.ts` и workflow `.github/workflows/deploy.yml`; локальная сборка `npm run build` успешна

- 2026-02-19: исправлена загрузка SVG-иконок бокового меню на GitHub Pages: пути в `src/assets.ts` переведены с `/icons/...` на `icons/...`; `npm run build` успешен

- 2026-02-19: заменена иконка `Google Meet` на локальный SVG (`public/icons/google-meet.svg`) и подключена через `src/assets.ts`; `npm run build` успешен

- 2026-02-19: в popover `Фильтры` добавлена стабильная подсветка открытого пункта первого уровня (`Источник/Авторы`) через класс `is-submenu-open`; сборка `npm run build` успешна

- 2026-02-19: локально убран нижний divider под заголовками `Поиск/Встречи/Интеграции/Настройки` (удален `border-bottom` у `.topbar`); `npm run build` успешен; без push по запросу

- 2026-02-19: в поле поиска заменена иконка `cancel` (символ `×` -> `public/icons/cancel.svg`), обновлены стили `search-clear-btn`; `npm run build` успешен; без push по запросу

- 2026-02-19: на экране `Встречи` добавлено состояние `isFiltering` и переиспользование `MeetingsSkeleton` при применении фильтров; `npm run build` успешен; без push
- 2026-02-19: в подпоповере фильтра `Авторы` добавлен верхний контрол поиска `Найти` (по Figma `27210:7129`) и фильтрация списка авторов по введенной строке; список и чекбоксы сохранены; `npm run build` успешен
- 2026-02-19: для поиска в подпоповере `Авторы` добавлено empty-state `Ничего не найдено` по Figma (`27210:7612`) при отсутствии совпадений; `npm run build` успешен
- 2026-02-19: `MeetingsSkeleton` переведен на динамическое количество строк по высоте viewport (до 7 строк вместо фиксированных 3) для экранов `Поиск` и `Встречи`; `npm run build` успешен
- 2026-02-19: в подпоповере фильтра `Источник` добавлен поиск `Найти` и empty-state `Ничего не найдено` по тому же UX-паттерну, что в `Авторы`; `npm run build` успешен

- 2026-02-19: в календарном контроле фильтра `Дата` кнопка подтверждения переименована с `Выбрать` на `Применить`; `npm run build` успешен

- 2026-02-19: в календаре фильтра `Дата` дни недели переведены на русский (`Пн Вт Ср Чт Пт Сб Вс`) в `FilterMenuButton`; `npm run build` успешен; без push по запросу

- 2026-02-20: глобально облегчены шрифты в `styles.css` (все `font-weight: 500` заменены на `400`), добавлено сглаживание (`-webkit-font-smoothing`, `-moz-osx-font-smoothing`); `npm run build` успешен; без push

- 2026-02-20: по согласованному списку точечно возвращен `font-weight: 500` для ключевых элементов UI (в `styles.css`), остальные веса оставлены без изменений; `npm run build` успешен; без push

- 2026-02-20: в календарном подпоповере `Дата` возвращен `font-weight: 500` для `.filters-date-title` и `.filters-date-apply`; `npm run build` успешен; без push

- 2026-02-20: в `applyFilters` добавлена сортировка встреч внутри дня по убыванию времени (`b.time.localeCompare(a.time)`); `npm run build` успешен; без push

- 2026-02-20: в мок-данных встреч подпись даты изменена с `17 фев` на `17 февраля`; `npm run build` успешен; без push

- 2026-02-20: исправлен cross-month рендер голубой заливки диапазона в календаре фильтра `Дата` (клиппинг range на видимую сетку месяца в `buildRangeSegments`); `npm run build` успешен; без push

- 2026-02-20: увеличен размер шрифта `plan-usage` с 10px до 12px в `styles.css`; `npm run build` успешен; без push

- 2026-02-20: у кнопки `Фильтры` добавлено постоянное состояние заливки `#F7F7F8` при `aria-expanded="true"`; `npm run build` успешен; без push

- 2026-02-20: у встречи `Синк с интеграцией Zoom и webhook-событиями` время изменено с `17:15` на `12:00` в `data.ts`; `npm run build` успешен; без push
- 2026-02-20: для встречи `Инцидент-ретро по задержке загрузок` (source `salutejazz`) превью изменено с `thumb: "legacy"` на `thumb: "audio1"`, чтобы убрать конфликт иконки в превью; `npm run build` успешен; без push
- 2026-02-20: превью встреч выровнено с Figma-нодой `27219:9287`: для всех встреч вне состояния `processing` в превью показывается иконка текущего источника (`source.icon`) на сером фоне; устранён конфликт для `SaluteJazz`; `npm run build` успешен; без push
- 2026-02-20: добавлено 5 новых мок-встреч (m11..m15) в `data.ts` с разными источниками/участниками/временем для более реалистичной проверки поиска и фильтров; `npm run build` успешен; без push
- 2026-02-20: новые встречи `m11..m15` разнесены по отдельным дням, включая январские даты (`2026-01-15`, `2026-01-09`, `2026-01-03`) для более реалистичной выборки фильтрации/поиска; `npm run build` успешен; без push
- 2026-02-20: исправлено переполнение email в подпоповере `Авторы`: для `.filters-source-label` добавлены `flex: 1`, `min-width: 0`, `overflow: hidden`, `text-overflow: ellipsis`; `npm run build` успешен; без push
- 2026-02-20: восстановлено левое выравнивание в строках подпоповеров `Источник/Авторы` после ellipsis-фикса: добавлен `text-align: left` для `.filters-source-item` и `.filters-source-label`; `npm run build` успешен; без push
