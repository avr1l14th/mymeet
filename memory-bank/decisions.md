# Decisions

## Формат записи
- Дата: `YYYY-MM-DD`
- Решение: кратко
- Почему: 1-2 причины
- Риск: что может пойти не так

## Журнал решений
- 2026-02-18
  - Решение: создать `memory-bank` как единый источник контекста проекта
  - Почему: уменьшает потери контекста между сессиями и ускоряет диагностику
  - Риск: устаревание записей без регулярного обновления
- 2026-02-18
  - Решение: строить цифровой двойник `mymeet.ai` поэтапно через ключевые экраны
  - Почему: проще контролировать качество и сохранять стабильность интерфейса
  - Риск: при отсутствии строгой фиксации этапов можно потерять целостность UX
- 2026-02-18
  - Решение: боковое меню и базовый каркас интерфейса считать неизменными по умолчанию
  - Почему: это снижает риск регрессий при добавлении новых разделов
  - Риск: слишком жесткий каркас может замедлить изменения, если не пересматривать его осознанно
- 2026-02-18
  - Решение: убрать хардкод навигации и использовать единый реестр экранов в `App`
  - Почему: новые экраны добавляются в одном месте и меньше риск поломок существующего поведения
  - Риск: при неверной конфигурации реестра экран может стать недоступным
- 2026-02-18
  - Решение: экран `Meetings` реализовать по Figma-узлу `27141:2899` с табами и вариативными строками списка
  - Почему: это закрывает этап 1 и выравнивает UI с целевым визуалом продукта
  - Риск: мок-данные могут отличаться от будущих реальных данных API
- 2026-02-19
  - Решение: добавить контрол `Фильтры` справа в строке табов на экране `Meetings`, используя паттерн с экрана `Search`
  - Почему: это выравнивает расположение контрола с актуальным Figma-узлом `27205:5851` и сохраняет единый UX между экранами
  - Риск: отдельный экран фильтров пока остается промежуточным шагом до возможного встраивания правой панели
- 2026-02-19
  - Решение: заменить переход на отдельный экран `filters` на единый popover-контрол по Figma-узлу `27161:8402` для экранов `Search` и `Meetings`
  - Почему: это соответствует дизайну (меню у кнопки `Фильтры`) и убирает разрыв поведения между двумя экранами
  - Риск: пункты `Источник/Авторы/Дата` пока отображаются как меню-уровень 1 без вложенных раскрытий
- 2026-02-19
  - Решение: для пункта `Источник` добавить hover-раскрытие финального popover-списка по Figma-узлу `27161:8460`
  - Почему: это точное поведение и компоновка из дизайна для этапа многоуровневой фильтрации
  - Риск: состояния выбора источников (checked/счетчик) пока не подключены к данным фильтрации
- 2026-02-19
  - Решение: подключить реальную фильтрацию по источникам как multi-select с единым состоянием для экранов `Поиск` и `Встречи`
  - Почему: это приводит поведение popover к Figma (`27205:7320`) и делает индикаторы (`чекбокс`, `точка`, бейдж `1`, `Очистить фильтры`) функциональными
  - Риск: старый экран `MeetingFiltersScreen` использовал одиночный source-фильтр и требовал совместимой адаптации под новый тип

- 2026-02-19
  - Решение: подготовить проект к GitHub Pages через GitHub Actions с `base: "/mymeet/"`
  - Почему: это безопасный и бесплатный способ публикации с автодеплоем на каждый push в `main`
  - Риск: без включения `Settings -> Pages -> Source: GitHub Actions` деплой не начнется

- 2026-02-19
  - Решение: для локальных SVG-иконок меню использовать относительные пути `icons/...` вместо абсолютных `/icons/...`
  - Почему: на GitHub Pages проект обслуживается из подпути `/mymeet/`, и абсолютные пути ломают загрузку ассетов
  - Риск: при переходе на вложенные клиентские маршруты без hash-router относительные пути могут потребовать отдельной проверки

- 2026-02-19
  - Решение: заменить источник иконки `Google Meet` на локальный SVG `public/icons/google-meet.svg`
  - Почему: удаленный ассет отображался растянутым; локальный SVG фиксирует корректные пропорции 14x14
  - Риск: при изменении имени файла потребуется синхронно обновить путь в `src/assets.ts`

- 2026-02-19
  - Решение: сохранять фоновую заливку у пункта `Источник/Авторы` первого уровня, пока открыт соответствующий подпоповер
  - Почему: это выравнивает поведение с Figma (одновременная подсветка открытого пункта уровня 1 и hover пункта уровня 2)
  - Риск: минимальный; добавлен только визуальный state-класс без изменения логики фильтрации

- 2026-02-19
  - Решение: убрать разделитель `#EFEFEF` под заголовком экранов через удаление `border-bottom` у `.topbar`
  - Почему: по актуальному UI-требованию заголовок должен быть без нижней серой линии на всех основных экранах
  - Риск: визуально блоки под шапкой могут выглядеть менее отделенными на некоторых разрешениях

- 2026-02-19
  - Решение: заменить символ `×` в очистке поиска на локальную SVG-иконку `cancel.svg`
  - Почему: новый SVG обеспечивает нужный размер/контраст и ближе к макету
  - Риск: минимальный; затрагивается только визуал кнопки очистки поиска

- 2026-02-19
  - Решение: на экране `Встречи` показывать `MeetingsSkeleton` при изменении фильтров (`Источник/Авторы/Дата`) перед отображением результата
  - Почему: это выравнивает UX с экраном `Поиск` и дает явную обратную связь при фильтрации
  - Риск: добавлена задержка 700мс перед отображением результатов, что может восприниматься как более медленная фильтрация
- 2026-02-19
  - Решение: заменить legacy-фильтр `dateRange` (`all/last7/last30/older`) на точную модель даты (`none/single/range`) и реализовать календарный подпоповер `Дата` по Figma (`default`, `single`, `range`) с подтверждением `Выбрать`
  - Почему: это напрямую соответствует дизайну и позволяет фильтровать встречи по конкретной дате или диапазону вместо относительных периодов
  - Риск: логика выбора диапазона построена как `single -> range -> single`; если продукту потребуется иной UX (например, повторный клик сбрасывает выбор), понадобится отдельная доработка
- 2026-02-19
  - Решение: добавить в подпоповер `Авторы` встроенный поиск `Найти` с фильтрацией текущего списка авторов по `label`
  - Почему: в длинных списках авторов это ускоряет выбор и соответствует верхнему контролу из Figma-узла `27210:7129`
  - Риск: при пустом результате сейчас показывается пустой список без отдельного текста-состояния
- 2026-02-19
  - Решение: добавить в подпоповер `Авторы` пустое состояние `Ничего не найдено` по Figma-узлу `27210:7612` при пустом результате поиска
  - Почему: это завершает UX поиска и устраняет «немое» пустое состояние без обратной связи
  - Риск: минимальный; добавлен только условный рендер одной строки, без изменения логики фильтрации
- 2026-02-19
  - Решение: заменить фиксированный рендер 3 skeleton-строк на динамический расчет по высоте viewport (ограничение до 7 строк)
  - Почему: это заполняет видимую область экрана на `Поиск` и `Встречи` и убирает «обрыв» skeleton после 3 строк
  - Риск: при нестандартной высоте окна расчет может дать больше визуальной нагрузки; ограничено верхним пределом 7 строк
- 2026-02-19
  - Решение: добавить в подпоповер `Источник` такой же поиск и empty-state, как в `Авторы`
  - Почему: длинный список источников требует быстрого поиска, а единый UX-паттерн снижает когнитивную нагрузку
  - Риск: минимальный; добавлены только локальные состояния поиска и условный рендер в подпоповере

- 2026-02-19
  - Решение: переименовать кнопку подтверждения даты в календаре с `Выбрать` на `Применить`
  - Почему: это точнее отражает действие применения фильтра и выравнивает копирайт в UI
  - Риск: минимальный; изменена только текстовая метка кнопки

- 2026-02-19
  - Решение: локализовать подписи дней недели в календаре фильтра `Дата` (`Mo..Su` -> `Пн..Вс`)
  - Почему: это выравнивает язык UI и устраняет смешение локалей в одном интерфейсе
  - Риск: минимальный; меняется только текстовая метка дней

- 2026-02-20
  - Решение: выполнить глобальное облегчение типографики UI (`font-weight: 500 -> 400`) и включить системное сглаживание шрифтов
  - Почему: фактический рендер в браузере выглядел визуально тяжелее макета Figma (особенно на мелких размерах 13px)
  - Риск: часть акцентных элементов станет менее выразительной и может потребовать точечной компенсации

- 2026-02-20
  - Решение: выполнить точечный возврат `font-weight: 500` только для согласованного списка элементов (заголовки экранов, активный таб, кнопка `Добавить встречу`, названия встреч, черная часть даты, `Mymeet.ai`, `Pro plan`, `Доступно 1850 из 2500`, заголовки empty/error)
  - Почему: это возвращает акцентную иерархию текста по макету без полного отката облегчения шрифтов
  - Риск: если позже потребуется полное совпадение по типографике, может понадобиться отдельная правка для оставшихся нейтральных элементов

- 2026-02-20
  - Решение: вернуть `font-weight: 500` в календаре фильтра `Дата` для заголовка месяца/года и кнопки `Применить`
  - Почему: эти элементы входят в согласованный список акцентной типографики по макету
  - Риск: минимальный; затронуты только 2 селектора

- 2026-02-20
  - Решение: сортировать встречи внутри каждого дня по убыванию времени (`HH:MM`) после применения фильтров
  - Почему: это обеспечивает предсказуемый порядок списка (самые поздние встречи сверху) независимо от исходного порядка данных
  - Риск: при переходе на иной формат времени (не `HH:MM`) comparator потребует адаптации

- 2026-02-20
  - Решение: заменить сокращенную подпись даты `17 фев` на полную форму `17 февраля` в данных списка встреч
  - Почему: это выравнивает язык UI с ожидаемой русской формой месяца в родительном падеже
  - Риск: минимальный; правка затрагивает только текст метки даты

- 2026-02-20
  - Решение: для календарного range-overlay применить клиппинг диапазона по видимой 42-дневной сетке месяца (`max(start, firstVisible)`..`min(end, lastVisible)`), чтобы сохранять заливку при переходе через границу месяцев
  - Почему: прежний расчет требовал присутствия обоих концов диапазона в текущем месячном grid и в cross-month кейсе возвращал пустой overlay
  - Риск: минимальный; изменен только расчет сегментов overlay без изменения модели даты и классов состояний дней

- 2026-02-20
  - Решение: увеличить размер текста `plan-usage` (`Доступно 1850 из 2500`) с 10px до 12px
  - Почему: по визуальному требованию текст должен быть читаемее в блоке `Pro plan`
  - Риск: минимальный; может незначительно измениться баланс в строке заголовка тарифа

- 2026-02-20
  - Решение: закрепить hover-заливку `#F7F7F8` у кнопки `Фильтры` в открытом состоянии через CSS-селектор `[aria-expanded="true"]`
  - Почему: при открытом popover контрол должен оставаться визуально активным по макету
  - Риск: минимальный; затронута только кнопка `Фильтры`, логика меню не меняется

- 2026-02-20
  - Решение: изменить время встречи `Синк с интеграцией Zoom и webhook-событиями` с `17:15` на `12:00`
  - Почему: чтобы встреча в обработке (`14:30`, `55%`) отображалась первой в списке дня после сортировки по убыванию времени
  - Риск: минимальный; затронуто только мок-значение времени одной встречи

- 2026-02-20
  - Решение: точечно заменить тип превью у встречи `Инцидент-ретро по задержке загрузок` с `legacy` на `audio1`
  - Почему: `legacy` использует фиксированную иконку превью, которая визуально конфликтовала с источником `SaluteJazz`
  - Риск: минимальный; затронута только одна запись мок-данных без изменения общей логики рендера

- 2026-02-20
  - Решение: в списке встреч рендерить превью от источника (`source.icon`) для всех непроцессинговых строк вместо фиксированной legacy/audio-иконки
  - Почему: по макету Figma `27219:9287` превью “старых встреч” должно отражать конкретный источник, иначе возникает визуальный конфликт (например, `SaluteJazz`)
  - Риск: текущие декоративные аудио-превью в обычном состоянии больше не используются; сохранён только `processing`-вариант с прогрессом

- 2026-02-20
  - Решение: расширить набор мок-данных на 5 встреч с разнообразными темами, участниками и источниками
  - Почему: это повышает вариативность выборки и делает проверку поиска/фильтров более показательной
  - Риск: минимальный; затронуты только тестовые данные без изменений логики

- 2026-02-20
  - Решение: распределить дополнительные мок-встречи по разным дням с вынесением части в январь 2026
  - Почему: пользователь попросил сделать хронологию менее однотипной и усилить «живость» поиска/фильтров на разных датах
  - Риск: минимальный; изменены только тестовые даты/группировка, логика приложения не затронута

- 2026-02-20
  - Решение: применить `ellipsis` для текстового лейбла автора/источника в строке фильтра
  - Почему: длинные email выходили за границы контейнера и ломали аккуратность списка
  - Риск: минимальный; длинный текст теперь обрезается визуально, полная строка остаётся в данных

- 2026-02-20
  - Решение: принудительно задать левое выравнивание текста в элементах списка фильтров
  - Почему: после добавления `flex: 1` для ellipsis текст начал наследовать центровку кнопки и визуально «уплыл» в центр
  - Риск: минимальный; правка только выравнивания, логика фильтров не затронута
